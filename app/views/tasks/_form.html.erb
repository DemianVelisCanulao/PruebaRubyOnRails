<h1>Nueva tarea</h1>

<%= form_with(model: [@project, @task], url: project_tasks_path(@project), data: {action: "submit->tasks#validate"}) do  |f| %>
   
    <%= f.label :title, 'Título' %>
    <%= f.text_field :title, :id => "task_title" %>

    <%= f.submit 'Enviar', disable_with: 'Enviando...' %>
<% end %>

